#!/usr/bin/env bash

# after linuxProfileSetup.sh has been run, you can use this file to apply all the
# changes without having to restart the shell. The file should be run via 
# the source command:

# source applyChanges.sh

source ~/.bashrc
